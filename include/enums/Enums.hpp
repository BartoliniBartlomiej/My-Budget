#pragma once

enum TransactionType {
    INCOME,
    EXPENSE
};

enum RecurrenceInterval {
    NONE,
    DAILY,
    WEEKLY,
    MONTHLY,
    YEARLY
};

enum CategoryType {
    FOOD,
    HOUSING,
    TRANSPORT,
    HEALTH,
    ENTERTAINMENT,
    SALARY,
    OTHER
};