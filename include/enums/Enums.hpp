#pragma once

enum TransactionType {
    INCOME,
    EXPENSE
};

enum RecurrenceInterval {
    NONE,
    DAILY,
    WEEKLY,
    YEARLY
};

enum CategoryType {
    FOOD,
    HOUSING,
    TRANSPORT,
    HEALTH,
    ENTERTAINMENT,
    SALARY,
    OTHER
};